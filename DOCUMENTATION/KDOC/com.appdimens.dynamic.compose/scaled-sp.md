//[AppDimens SDP, HDP, WDP: Scalable Width and Height Dimensions](../../index.md)/[com.appdimens.dynamic.compose](index.md)/[scaledSp](scaled-sp.md)

# scaledSp

fun ERROR CLASS: Symbol not found for TextUnit.[scaledSp](scaled-sp.md)(): [ScaledSp](-scaled-sp/index.md)

EN Starts the build chain for ScaledSp from a base TextUnit (Sp). Usage example: `16.sp.scaledSp().screen(...)`.

PT Inicia a cadeia de construção para ScaledSp a partir de um TextUnit (Sp) base. Exemplo de uso: `16.sp.scaledSp().screen(...)`.

EN Starts the build chain for ScaledSp from a base Int (treated as sp). Usage example: `16.scaledSp().screen(...)`.

PT Inicia a cadeia de construção para ScaledSp a partir de um Int base (tratado como sp). Exemplo de uso: `16.scaledSp().screen(...)`.
